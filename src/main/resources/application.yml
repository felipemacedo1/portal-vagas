spring:
  profiles:
    active: dev
  application:
    name: portal-vagas

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:postgresql://localhost:5432/portal_vagas_dev
    username: ${DB_USERNAME:portal_user}
    password: ${DB_PASSWORD:portal_pass}
    driver-class-name: org.postgresql.Driver

server:
  port: 8081
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET:dev-secret-key-change-in-production-min-256-bits}
  access-token-expiration: 3600000  # 1 hour
  refresh-token-expiration: 86400000 # 24 hours

mail:
  host: smtp.gmail.com
  port: 587
  username: ${MAIL_USERNAME:}
  password: ${MAIL_PASSWORD:}
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
  from: ${MAIL_FROM:noreply@portalvagas.com}

logging:
  level:
    com.portalvagas: DEBUG
    org.springframework.security: DEBUG

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DATABASE_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_EXPIRATION:3600000}
  refresh-token-expiration: ${JWT_REFRESH_EXPIRATION:86400000}

mail:
  host: ${MAIL_HOST:smtp.gmail.com}
  port: ${MAIL_PORT:587}
  username: ${MAIL_USERNAME}
  password: ${MAIL_PASSWORD}
  properties:
    mail:
      smtp:
        auth: true
        starttls:
          enable: true
  from: ${MAIL_FROM}

logging:
  level:
    root: INFO